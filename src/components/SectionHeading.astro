---
interface Props {
  eyebrow: string;
  title: string;
}
const { eyebrow, title } = Astro.props;
---

<div class="reveal">
  <p class="text-sm font-medium tracking-widest text-accent uppercase">{eyebrow}</p>
  <h2 class="mt-3 font-display text-3xl tracking-tight sm:text-4xl">{title}</h2>
</div>
